/// Defines visual size of Select component
enum SelectSize { sm, md, lg }

/// Defines visual style variant of Select
enum SelectVariant { outline, filled, ghost }

/// Defines selection mode
enum SelectMode { single, multi }
